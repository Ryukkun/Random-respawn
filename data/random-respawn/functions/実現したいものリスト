・地図は持ってるかもって無いかで自動的に判別 storage 使用
    ・地図意外だった場合も自動で無効

・自動設定機能（おすすめ）
    ・人数によって自動的に範囲が割り当てられる
    ・map-centerは自動的に探される
    ・地図を必要か確認し、必要だったらmap-centerに飛ばして地図を作らせる
    ・

・✓ GM=4の時はspreadtestしない

・Play中にアイテムを投げられないように 消えないようにする GM=4 の時だけ
        ⇒ map-centerをアーマースタンドにし、地図を持たせる
        ⇒ 所定のインベントリが地図以外の状態になったら、その場にアイテム召喚(ディレイ入れておく) 空気だった場合はパス
            ⇒ dataで@p[distance=..0]のそのインベントリからコピー
            ⇒ @p[re]のUUIDをOwnerに代入
        ⇒ map-centerから地図をぶち込む

・Play後に回覧できるようにSpawnpointを召喚しておく 出来ればCustomNameも
        ⇒ forceloadしておく
        ⇒ member作って同じにしておく
        ⇒ 試合終了とともに発光とCustom Name 出す

・もし初期スポーン地点に戻ってしてしまった倍の事を考え、初期スポの位置を記録しておき、Play中に初期リスに来た奴はmemberが同じ奴のspawnpoinに飛ばす

・reset用ファイルを作っておく

・詳細設定
    ・観戦はクリエイティブか、スペクテーターか
    ・時間
    ・範囲
    ・

・actionbar

・時間が来たらここがmapの範囲外かなどを表示させる

・本